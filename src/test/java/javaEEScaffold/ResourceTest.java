package javaEEScaffold;

import static org.junit.Assert.*;

import org.junit.After;
import org.junit.AfterClass;
import org.junit.Before;
import org.junit.Test;
import org.springframework.context.support.ClassPathXmlApplicationContext;

import com.vino.scaffold.shiro.entity.Resource;
import com.vino.scaffold.shiro.service.ResourceService;

public class ResourceTest {
	// private  ClassPathXmlApplicationContext ctx ;
	// private ResourceService resourceService;
	//
	// @Before
	// public void setUp() throws Exception {
	// 	 // ctx =  new ClassPathXmlApplicationContext("applicationContext.xml");
	// 	// resourceService=ctx.getBean("resourceService",ResourceService.class);
	// }
	//
	// @After
	// public void tearDown() throws Exception {
	// 	// ctx.close();
	// }
	//
	// @Test
	// public void testSave() {
	// 	// resourceService.save(new Resource("��ɫ����", "menu", "role:menu"));
	// 	// resourceService.save(new Resource("������ɫ", "button", "role:create"));
	// 	// resourceService.save(new Resource("ɾ����ɫ", "button", "role:delete"));
	// 	// resourceService.save(new Resource("�༭��ɫ", "button", "role:update"));
	// 	// resourceService.save(new Resource("��ѯ��ɫ", "button", "role:view"));
	// }

}
